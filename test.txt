program demo;
var A, B, C, D, E, F, G, H, J, K: float;

int myFunc(i: int) [
    var j: int; k, l: float;
    {
        print(j, k, l);
        return j + i;
    }
];

void myFunc1(y: int, x: int) [
    var j: int; k, l: float;
    {
        print(j, k, l);
        return;
    }
];

main {
    A = B + C * (D-E/F) * H;
    B = E - F;
    while (A*B >= D*E/(G*H)) do {
        H = J * K + B;
        if (B < H){
            B = H + J;
            while (B > A + C) do {
                print(A+B*C, D-E);
            };
        }
        else{
            while (A-D < C+B) do {
                A = A + B;
                print(B-D);
            };
        };
    };

    myFunc1(myFunc(1), 3);
}
end