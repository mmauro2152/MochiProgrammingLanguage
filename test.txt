program myProgram;
var x: int; y: float;

float myMathFunc(n: float) [
var k, l, o: int; a, b: string;
{
    n = n * 3 + 2 / 3 + (n * 24 / 3.5);
    n = k = l = - (k + l);
}];

main {
    x = 10;

    myMathFunc(5.3);
    y = 3.1;
    print(x + y, x, y, x / y, myMathFunc(5.3));
}
end