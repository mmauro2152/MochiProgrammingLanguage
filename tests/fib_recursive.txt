program fib_recursive;
var n, i: int;

int fib(x: int) [{
    if (x <= 1) {
        return x;
    };
    return fib(x - 1) + fib(x - 2);
}];

main {
    n = 10;
    i = 0;

    while (i < n) do {
        print(i, ": ", fib(i));
        i++;
    };
}
end